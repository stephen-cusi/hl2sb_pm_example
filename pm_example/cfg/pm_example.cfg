// This is the main script
echo "Player models are loading..."
// The first time is the pre-caching operation
cl_playermodel models/
// The second time is to officially switch the player model. If the model is placed in a certain folder of custom, a new script needs to be executed.
cl_playermodel models/
echo "The second switching operation is in progress"
// Execute the second script
exec "pm_example_"